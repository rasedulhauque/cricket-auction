version: '3.8'

services:
  cricket-auction:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    volumes:
      - ./data:/app/data   # persists team + game JSON files across container restarts
    restart: unless-stopped
